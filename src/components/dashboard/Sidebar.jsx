import { NavLink, useLocation, useNavigate } from 'react-router-dom'; import { Inbox, FolderOpen, CheckCircle2, Archive, Users, Settings, LogOut, Menu, X } from 'lucide-react'; export default function Sidebar({ collapsed, onToggle }) { var loc = useLocation(); var nav = useNavigate(); var session = JSON.parse(localStorage.getItem('ja_session') || '{}'); var isActive = function (path) { return loc.pathname === path || (path !== '/dashboard' && loc.pathname.startsWith(path)) }; var logout = function () { localStorage.removeItem('ja_session'); nav('/dashboard/login') }; return (<aside className={'sidebar' + (collapsed ? ' collapsed' : '')}><div className="sidebar-header"><span className="sidebar-logo">JA</span>{!collapsed && <span className="sidebar-brand">JASA AMANAH</span>}<button onClick={onToggle} className="sidebar-toggle">{collapsed ? <Menu size={20} /> : <X size={20} />}</button></div><nav className="sidebar-nav"><NavLink to="/dashboard" end className={ function ({ ipActive }) { return 'sidebar-link' + (ipActive ? ' active' : '') } }><Inbox size={20} />{!collapsed && <span>Inbox</span>}</NavLink><NavLink to="/dashboard/diproses" className={ function ({ isActive }) { return 'sidebar-link' + (isActive ? ' active' : '') } }><FolderOpen size={20} />{!collapsed && <span>Diproses</span>}</NavLink><NavLink to="/dashboard/selesai" className={ function ({ isActive }) { return 'sidebar-link' + (isActive ? ' active' : '') } }><CheckCircle2 size={20} />{!collapsed && <span>Selesai</span>}</NavLink>{session.role === 'admin' && <><NavLink to="/dashboard/staff" className={ function ({ isActive }) { return 'sidebar-link' + (isActive ? ' active' : '') } }><Users size={20} />{collapsed && <span>Staff</span>}</NavLink><NavLink to="/dashboard/pengaturan" className={ function ({ isActive }) { return 'sidebar-link' + (isActive ? ' active' : '') } }><Settings size={20} />{collapsed && <span>Pengaturan</span>}</NavLink></>}<button onClick={logout} className="sidebar-logout"><LogOut size={20} />{!collapsed && <span>Keluar</span>}</button></nav></aside>)
}