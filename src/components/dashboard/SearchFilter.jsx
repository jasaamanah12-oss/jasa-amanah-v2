import { useState } from 'react'; import { Search, Filter } from 'lucide-react'; export default function SearchFilter({ search, setSearch, statusFilter, setStatusFilter, categoryFilter, setCategoryFilter }) { var categories = ['KTP', 'Ijazah', 'KK', 'Akta Kelahiran', 'Akta Cerai', 'SIM', 'Paspor', 'NPWP', 'Sertifikat', 'Vaksin', 'Dokumen Lainnya']; var statuses = ['Menunggu', 'Diproses', 'Selesai']; return (<div className="search-filter"><div className="search-bar"><Search size={16} /><input type="text" placeholder="Cari pengajuan..." value={search} onChange={ function (e) { setSearch(e.target.value) } } /></div><div className="filter-bar"><Filter size={16} /><select value={statusFilter} onChange={ function (e) { setStatusFilter(e.target.value) } }><option value="">Semua Status</option>{statuses.map(function (s) { return <option key={s} value={s}>{s}</option> })}</select><select value={categoryFilter} onChange={ function (e) { setCategoryFilter(e.target.value) } }><option value="">Semua Dokumen</option>{categories.map(function (c) { return <option key={c} value={c}>{c}</option> })}</select></div></div>)
}