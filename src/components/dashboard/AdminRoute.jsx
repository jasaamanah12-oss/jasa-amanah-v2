import { Navigate } from 'react-router-dom'; export default function AdminRoute({ children }) { var session = localStorage.getItem('ja_session'); if (!session) return <Navigate to="/dashboard/login" replace />; try { var data = JSON.parse(session); if (data.role !== 'admin') return <Navigate to="/dashboard" replace /> } catch (e) { return <Navigate to="/dashboard/login" replace /> } return children }
