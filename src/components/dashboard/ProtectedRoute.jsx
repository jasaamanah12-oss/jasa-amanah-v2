import { Navigate } from 'react-router-dom'; export default function ProtectedRoute({ children }) { var session = localStorage.getItem('ja_session'); if (!session) return <Navigate to="/dashboard/login" replace />; try { var data = JSON.parse(session); if (!data.id || !data.role) return <Navigate to="/dashboard/login" replace /> } catch (e) { return <Navigate to="/dashboard/login" replace /> } return children }
