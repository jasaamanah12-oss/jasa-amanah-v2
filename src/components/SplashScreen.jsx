import { useEffect, useState } from 'react';import logoImg from '../public/logo-jasa-amanah.png';export default function SplashScreen() { var [visible, sV] = useState(true); var [particles, sP] = useState([]); useEffect(function () { var p = []; for (var i = 0; i < 8; i++) p.push({ id: i, x: Math.random() * 100, y: Math.random() * 100, s: 2 + Math.random() * 4, dx: (Math.random() - 0.5) * 0.6, dy: (Math.random() - 0.5) * 0.6, delay: Math.random() * 1.5 }); sP(p); var t = setTimeout(function () { sV(false) }, 3000); return function () { clearTimeout(t) } }, []); if (!visible) return null; return (<div className="splash-screen">{particles.map(function (p) { return (<div key={p.id} className="splash-particle" style={{ left: p.x + '%', top: p.y + '%', width: p.s + 'px', height: p.s + 'px', animationDelay: p.delay + 's' }} />) })}<div className="splash-logo-wrap"><div className="splash-logo-sweep" /><img src={logoImg} alt="JASA AMANAH" className="splash-logo" /><div className="splash-glow" /></div></div>)
}