import { useState } from 'react'; import { Settings } from 'lucide-react'; export default function Pengaturan() { var [msg, sM] = useState(''); var handleSave = function (e) { e.preventDefault(); sM('Pengaturan disimpan!'); setTimeout(function () { sM('') }, 3000) }; return (<div className="pengaturan"><div className="dashboard-header"><Settings size={24} /><h2>Pengaturan</h2></div><form onSubmit={handleSave} className="form-setting"><div className="form-group"><label>Nomor WhatsApp</label><input type="text" defaultValue="089507527206" /></div><button type="submit" className="btn-primary">Simpan</button>{msg && <p className="msg-success">{msg}</p>}</form></div>)
}