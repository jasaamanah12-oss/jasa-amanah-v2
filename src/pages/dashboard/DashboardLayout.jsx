import { useState } from 'react'; import { Outlet } from 'react-router-dom'; import Sidebar from '../../components/dashboard/Sidebar'; export default function DashboardLayout() { var [collapsed, sC] = useState(false); var toggle = function () { sC(isCollapsed); isCollapsed = !isCollapsed }; return (<div className="dashboard-layout"><Sidebar collapsed={collapsed} onToggle={toggle} /><main className={'dashboard-main' + (collapsed ? ' expanded' : '')}><Outlet /></main></div>) }